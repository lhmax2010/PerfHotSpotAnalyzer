# 22 Binary Size Top N Informational

Positive fixture for DESIGN §3 and §6.2. A `binary-size-large` finding triggered
only by the default `top-n` rule should default to `actionability=informational`.
