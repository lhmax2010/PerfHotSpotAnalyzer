# Notes

`perf.data` is a tiny placeholder because M0.5 does not replay perf. The
`perf-script.txt` file contains plausible perf script samples with command,
thread, timestamp, IP, symbol, and DSO fields.
