# 14 Ownership Profile

Positive fixture for DESIGN §3B.1. It provides a Tizen-oriented
`ownership.yaml` template with `/usr/lib`, `/usr/lib64`, `/lib`, and `/lib64`
patterns, plus owned/third-party/system/unknown examples.
