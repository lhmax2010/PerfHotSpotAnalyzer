# 09 Benchmark Regression Missing Comparison Baseline

Negative fixture for DESIGN §10.4 item ⑨. It must be rejected because a
`benchmark-regression` finding requires top-level `comparison.baseline_report`.

