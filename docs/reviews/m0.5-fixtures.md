# M0.5 Fixtures Review Package

## Scope

M0.5 adds the DESIGN §10.4 golden fixture baseline and a functional test that
validates every fixture through `common/schema_validate.py`.

No schemas, tracing code, CLI behavior, A/B business logic, Tizen device logic,
or runtime patch application behavior were added in this stage.

## Key Files

- `tests/fixtures/golden/`
- `tests/functional/test_golden_fixtures.py`
- `docs/test-guides/m0.5-fixtures.md`
- `.dev_memory/m0.5_fixtures/*`

## Fixture Coverage

Positive:

- ① hotspot + binary-size mixed
- ② Google Benchmark before/after
- ③ Google Benchmark single latency
- ④ binary-size-regression
- ⑤ low-confidence generic-llm
- ⑥ advisory-only patch
- ⑦ diff-ready patch

Negative:

- ⑧ binary-size-regression missing baseline
- ⑨ benchmark-regression missing `comparison.baseline_report`
- ⑩ advisory-only with diff
- ⑪ diff-ready missing `chosen_anchor`
- ⑫ binary-size-large missing threshold

## Validation

```bash
.venv/bin/python -m pytest tests/functional/test_golden_fixtures.py -q
```

Result: 12 passed.

```bash
.venv/bin/python -m pytest
```

Result: 32 passed.

CLI smoke also confirmed a positive fixture exits `0` and a negative fixture is
rejected with exit `1`.

## Review Checklist

- [x] Positive fixtures ①-⑦ all pass.
- [x] Negative fixtures ⑧-⑫ are all rejected by `schema_validate`.
- [x] Every fixture has `README.md`, `notes.md`, and `expected.json`.
- [x] No M0.5+ business logic was implemented.
- [x] No Python code calls LLM APIs.
- [x] No runtime patch is applied, committed, or pushed.

## Next Stage

After M0.5 review and merge, start A1 and/or B1 from reviewed `main`.
